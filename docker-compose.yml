version: '3.9'
services:
  app:
    build: .
    image: household-command-center:latest
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/app:cached
      - /app/node_modules
    command: npm run dev

  # For production, run the built image with `docker-compose -f docker-compose.yml run --service-ports app`
